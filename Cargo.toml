[package]
name = "cursor-bridge"
version = "0.1.0"
edition = "2021"
description = "One binary. Claude Code on Cursor's backend. Zero config."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
